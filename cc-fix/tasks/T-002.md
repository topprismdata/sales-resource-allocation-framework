# T-002 路径去硬编码（Phase 1）

规格来源：`cc-fix/CONTRACTS.md`（**只读**）。本卡已内联全部所需信息。

## 目标

消灭 26 个文件中的 `/Users/ghb/...` 硬编码路径，使仓库在**任何机器、任何用户**下可运行。
这是审查认定的头号 P0 问题——当前代码仅在原作者本机可跑。

## 现状（已实测，直接采信，不要重新统计）

26 个文件命中 `/Users/ghb`，共 3 种模式：

| 模式 | 出现次数 | 原文 |
|---|---:|---|
| **ROOT** | 20 | `sys.path.insert(0, "/Users/ghb/sales-resource-allocation-framework")` |
| **DATA** | 20 | `DATA = "/Users/ghb/sales-resource-allocation-framework/data/gz"` |
| **DOWNLOADS** | 13 | `open('/Users/ghb/Downloads/<文件名>')` |

DOWNLOADS 涉及的文件（**均已存在于 `data/gz/source/`，字节一致**）：
- `边界数据-路网-到区县带海岸线-四级路网-广东省-广州市.geojson`（6 处）
- `区划数据-街道-广东省-广州市.geojson`（4 处）
- `广州办事处经销商围栏数据-20260827.csv`（2 处，改读 `../客户数据/`）
- 另有 1 处裸 `<DOWNLOADS>` 目录、2 处 `<DATA>/*.svg` 输出路径

关键链路命中数：`territory_compile.py` 5 处、`allocation_ledger.py` 1 处、
`yeidai_ops.py` 1 处；`demo_server.py` 与 `yeidai_compile.py` 各 0 处（已干净）。

## 实现要求

### 1. 新建 `tools/_paths.py`

```python
#!/usr/bin/env python3
"""统一路径解析——消灭硬编码用户目录。

优先级：显式参数 > 环境变量 SRAF_DATA_DIR > <仓库根>/data/gz
仓库根由 __file__ 推导，不依赖任何用户名。"""
import os
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent          # tools/ 的上一级 = 仓库根


def data_dir(arg: str | None = None) -> Path:
    if arg:
        return Path(arg)
    env = os.environ.get("SRAF_DATA_DIR")
    if env:
        return Path(env)
    return ROOT / "data" / "gz"


DATA = str(data_dir())                                  # 兼容既有 f"{DATA}/x.json" 写法
SOURCE = data_dir() / "source"                          # 原 ~/Downloads 的 geojson 落点
```

### 2. 替换规则（逐条对应，不要自创写法）

| 原文 | 替换为 |
|---|---|
| `sys.path.insert(0, "/Users/ghb/sales-resource-allocation-framework")` | `sys.path.insert(0, str(__import__("_paths").ROOT))`<br>或在已 import `_paths` 后用 `sys.path.insert(0, str(_paths.ROOT))` |
| `DATA = "/Users/ghb/.../data/gz"` | `from _paths import DATA`（或 `DATA = _paths.DATA`） |
| `open('/Users/ghb/Downloads/<x>.geojson')` | `open(_paths.SOURCE / "<x>.geojson")` |
| `open('/Users/ghb/Downloads/广州办事处经销商围栏数据-20260827.csv')` | `open(_paths.ROOT.parent / "客户数据" / "广州办事处经销商围栏数据-20260827.csv")` |

**注意 import 可行性**：脚本以 `python3 tools/xxx.py` 运行时 `sys.path[0]` 即 `tools/`，
故 `import _paths` 可直接工作。若某文件从别处被 import 导致找不到 `_paths`，
用 `sys.path.insert(0, str(Path(__file__).resolve().parent))` 兜底后再 import。

### 3. 分批处理

- **批 A（关键链路，逐个改并验证）**：`territory_compile.py`、`allocation_ledger.py`、
  `yeidai_ops.py`、`yeidai_compile.py`、`demo_server.py`
- **批 B（其余 21 个 bench/一次性脚本）**：同规则替换，只需保证语法正确与 grep 归零，
  **不要求能跑通**（它们多数依赖尚未重建的派生数据）

## 已知的非目标（不要试图解决，也不要因此阻塞）

`territory_compile.py:50` 读 `{DATA}/gz_osm_full.json`，该文件**当前不存在**，
需从 Overpass API 拉取。**本任务范围不含联网操作。**

→ 因此 `territory_compile.py` 改完后**仍会 import 失败**，但错误信息必须
从 `/Users/ghb/Downloads/...` 变为 `data/gz/gz_osm_full.json`。
**这个变化本身就是路径修复成功的证明**，见断言 B5。

## 验收断言（100% 门禁，逐条必须为真）

```bash
cd /Users/cai/Desktop/达能-SRP-AI/sraf

# B1 硬编码彻底归零
test "$(grep -rl '/Users/ghb' tools/ 2>/dev/null | wc -l | tr -d ' ')" = "0" \
  && echo "B1 PASS 无 /Users/ghb 残留" || { echo "B1 FAIL"; grep -rl '/Users/ghb' tools/; }

# B2 _paths.py 存在且不含任何用户名硬编码
python3 -c "
import sys; sys.path.insert(0,'tools')
import _paths, pathlib
assert _paths.ROOT.name=='sraf', _paths.ROOT
assert '/Users/ghb' not in pathlib.Path('tools/_paths.py').read_text()
assert _paths.data_dir().as_posix().endswith('data/gz')
assert _paths.data_dir('X/Y').as_posix()=='X/Y'
import os; os.environ['SRAF_DATA_DIR']='/tmp/zz'
assert _paths.data_dir().as_posix()=='/tmp/zz'
print('B2 PASS _paths 三级优先级正确')
"

# B3 Ledger 与 YeidaiState 的错误指向数据包内路径，而非 /Users/ghb
python3 -c "
import sys; sys.path.insert(0,'tools')
for mod,cls in [('allocation_ledger','Ledger'),('yeidai_ops','YeidaiState')]:
    m=__import__(mod)
    try:
        getattr(m,cls)()
        print(f'  {mod}.{cls} 构造成功')
    except FileNotFoundError as e:
        assert '/Users/ghb' not in str(e), f'{mod} 仍指向 ghb: {e}'
        assert 'data/gz' in str(e), f'{mod} 路径异常: {e}'
        print(f'  {mod}.{cls} 缺数据但路径正确: {str(e).split(chr(39))[-2]}')
print('B3 PASS 关键模块路径已本地化')
"

# B4 现有测试不回归（基线 23 passed）
python3 -m pytest tests/ -q 2>&1 | tail -3

# B5 territory_compile 的两个 geojson 已能从 data/gz/source 读到
#    （证明：错误应推进到 gz_osm_full.json，而非卡在 geojson）
python3 -c "
import sys; sys.path.insert(0,'tools')
try:
    import territory_compile
    print('B5 PASS territory_compile 完整 import 成功')
except FileNotFoundError as e:
    s=str(e)
    assert '/Users/ghb' not in s, f'仍指向 ghb: {s}'
    assert 'gz_osm_full' in s, f'未推进到预期位置，实际: {s}'
    print('B5 PASS 两个 geojson 已读通，错误如期推进到 gz_osm_full.json')
"

# B6 demo_server 启动报错不再含 /Users/ghb
python3 tools/demo_server.py --data-dir data/gz 8798 2>&1 | tail -5 > /tmp/b6.txt
grep -q '/Users/ghb' /tmp/b6.txt && { echo "B6 FAIL 仍含 ghb"; cat /tmp/b6.txt; } || echo "B6 PASS 启动报错已本地化"
```

**B1-B6 全部必须 PASS。**

## 禁止事项

- ❌ 不得修改 `cc-fix/CONTRACTS.md`
- ❌ **不得联网**（不得调用 Overpass / fetch_region_osm.py）
- ❌ 不得伪造 `gz_osm_full.json` 或任何缺失的派生数据
- ❌ 不得为通过断言而删除功能代码或注释掉调用
- ❌ 不得改变任何业务逻辑——**本任务纯粹是路径替换**

## 分级阻塞

| 歧义影响范围 | 处置 |
|---|---|
| 阻塞批 A（关键链路 5 文件） | 停止整卡，上报 |
| 仅影响批 B（21 个 bench 脚本） | **批 A 照常完成**，该文件单独标记待裁决 |

## 必须上报（禁止自行裁决）

1. 某处硬编码无法用上述 4 条规则覆盖（出现第 5 种模式）
2. 替换后 B4 出现测试回归（现有 23 条不再全绿）
3. 发现路径替换会改变业务语义
4. 任何断言无法满足且原因指向规格本身 → 上报，**不要放宽断言**

## 子代理

允许 `collaboration.spawn_agent` 并行处理批 B。**子代理同受本卡全部约束。**

## 完成后

1. 6 条断言实际输出写入 `cc-fix/verify/T-002-verify.txt`
2. `cc-fix/JOURNAL.md` 追加执行记录（含失败与被推翻的归因）
3. **不要 git commit**，由架构层独立验收后提交
